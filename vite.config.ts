import { defineConfig } from "vite";

// GitHub project page URL is https://<user>.github.io/<repo>/
// Keep this in sync with your GitHub repository name.
export default defineConfig({
  base: "/bidding-card/",
});
